
const isEmpty = (obj) => {
    return JSON.stringify(obj) === "{}";
}

const validateInput = (arr) => {
    //employee post
    if (arr.length === 8){
        if (arr[0] || arr[1])
    }
};

module.exports = {isEmpty};